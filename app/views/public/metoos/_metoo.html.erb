<% if Metoo.find_by(user_id: current_user.id, answer_id: answer.id) %>
<%= link_to "/public/questions/#{answer.question.id}/answers/#{answer.id}/metoos/#{answer.metoos.find_by(user_id: current_user.id, answer_id: answer.id).id}", method: :delete, remote: true do %>
<i class="far fa-handshake" aria-hidden="true" style="color: red;"></i>
<% end %>
<% else %>
<%= link_to "/public/questions/#{answer.question.id}/answers/#{answer.id}/metoos", method: :post, remote: true do %>
<i class="far fa-handshake" aria-hidden="true"></i>
<% end %>
<% end %>
<%= answer.metoos.count %>Me too