$(document).on('turbolinks:load', function() {
	$('.fa-handshake').on('click', function() {
		$("#metoo-<%= @answer.id %>").html("<%= j(render partial: 'users/metoo', locals: { answer: @answer }) %>");
	});
});